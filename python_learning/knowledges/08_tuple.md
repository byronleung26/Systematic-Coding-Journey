# 知识点
1. 元组tuple，不可变的列表
2. 使用()来标识
```python
   dimensions = (200, 50)
   print(dimensions[0])
   print(dimensions[1])
   ```
   - 试图修改元组的操作是被禁止的
   - 严格地说，元组是由逗号标识的，圆括号只是让元组看起来更整洁、更清晰。如果你要定义只包含一个元素的元组，必须在这个元素后面加上逗号
   - 使用 for 循环来遍历元组中的所有值
   - 给元组变量重新赋值